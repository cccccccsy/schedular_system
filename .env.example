# 应用配置
APP_NAME=智能刷题计划调度系统
APP_VERSION=0.1.0
DEBUG=True

# API 配置
API_V1_PREFIX=/api/v1

# 数据库配置（相对于项目根目录）
DATABASE_URL=sqlite+aiosqlite:///./data/database/app.db

# CORS 配置（逗号分隔）
CORS_ORIGINS=*

# 算法配置
DEFAULT_DAILY_TIME_BUDGET=60
MAX_PROBLEMS_PER_REQUEST=500
CACHE_TTL_SECONDS=3600
